import type { NextConfig } from "next";

// Next.js 的配置项。这里目前是空的（用默认配置）。
// 需要改 Next.js 行为时（比如开启图片优化、配置代理等），在这里加选项。
const nextConfig: NextConfig = {
  /* config options here */
};

// 导出配置，Next.js 会自动读取这个默认导出
export default nextConfig;
